 'use client';
import {useEffect,useState} from 'react';import AdminResource from '@/components/AdminResource';
const nav=[['dashboard','Dashboard'],['projects','Projects'],['gallery','Gallery'],['beforeafter','Before / After'],['services','Services'],['products','Products'],['testimonials','Testimonials'],['blog','Blog + SEO'],['enquiries','Leads / Enquiries'],['settings','SEO + Site Settings']];
export default function Dashboard(){const [tab,setTab]=useState('dashboard');const [stats,setStats]=useState<any>({});useEffect(()=>{fetch('/api/admin/summary').then(r=>r.json()).then(setStats)},[]);async function logout(){await fetch('/api/admin/logout',{method:'POST'});location.href='/admin/login'}return <div className="admin-wrap"><aside className="admin-side"><div className="admin-brand"><img src="/logo/virvaani-art-logo.png" alt=""/><b>VIRVAANI ART<br/>CONTROL ROOM</b></div><nav className="admin-nav">{nav.map(([id,label])=><button className={tab===id?'active':''} key={id} onClick={()=>setTab(id)}>{label}</button>)}<a href="/" target="_blank">↗ View Website</a><button onClick={logout}>Log out</button></nav></aside><main className="admin-main">{tab==='dashboard'?<><div className="admin-top"><div><h1>Control Room</h1><p className="admin-muted">Manage the content behind the existing VIRVAANI ART website.</p></div></div><div className="admin-grid">{[['projects','Projects'],['gallery','Gallery'],['services','Services'],['enquiries','New enquiries']].map(([k,l])=><div className="admin-stat" key={k}><b>{stats[k]??'—'}</b><span>{l}</span></div>)}</div><div className="admin-panel"><h2>How it works</h2><p className="admin-muted">Admin changes are saved in Prisma + SQLite. Public Projects, Services and Gallery pages read from the database, so you can update the website without editing code.</p></div></>:tab==='enquiries'?<Enquiries/>:tab==='settings'?<Settings/>:<AdminResource resource={tab as any}/>}</main></div>}
function Enquiries(){const [rows,setRows]=useState<any[]>([]);async function load(){const r=await fetch('/api/admin/enquiries');if(r.ok)setRows(await r.json())}useEffect(()=>{load()},[]);async function update(id:string,status:string){await fetch('/api/admin/enquiries',{method:'PUT',headers:{'Content-Type':'application/json'},body:JSON.stringify({id,status})});load()}async function del(id:string){if(confirm('Delete enquiry?')){await fetch('/api/admin/enquiries',{method:'DELETE',headers:{'Content-Type':'application/json'},body:JSON.stringify({id})});load()}}return <section className="admin-panel"><div className="admin-top"><div><h1>Leads / Enquiries</h1><p className="admin-muted">Every public contact form submission is saved here.</p></div></div><div className="admin-list">{rows.map(x=><div className="admin-row" key={x.id}><div><b>{x.name}</b><div className="admin-muted">{x.email} • {x.phone} • {x.city}</div><p>{x.message}</p></div><div className="admin-actions"><select value={x.status} onChange={e=>update(x.id,e.target.value)}><option>NEW</option><option>CONTACTED</option><option>CONVERTED</option><option>CLOSED</option></select><button className="admin-btn danger" onClick={()=>del(x.id)}>Delete</button></div></div>)}{!rows.length&&<p className="admin-muted">No enquiries yet.</p>}</div></section>}
function Settings(){const [form,setForm]=useState<any>({});const [msg,setMsg]=useState('');useEffect(()=>{fetch('/api/admin/settings').then(r=>r.json()).then(setForm)},[]);async function save(e:React.FormEvent){e.preventDefault();const r=await fetch('/api/admin/settings',{method:'PUT',headers:{'Content-Type':'application/json'},body:JSON.stringify(form)});setMsg(r.ok?'Settings saved.':'Unable to save.')}return <section className="admin-panel"><div className="admin-top"><div><h1>SEO + Site Settings</h1><p className="admin-muted">Update contact details and search metadata without changing the visual design.</p></div></div><form className="admin-form admin-form-grid" onSubmit={save}>{['siteName','tagline','description','email','phone','address','seoTitle','seoDescription','logo'].map(k=><label className={k==='description'||k==='address'||k==='seoDescription'?'full':''} key={k}>{k}<textarea value={form[k]||''} onChange={e=>setForm({...form,[k]:e.target.value})}/></label>)}<div className="full admin-actions-bar"><button className="admin-btn">Save Settings</button></div>{msg&&<div className="admin-message full">{msg}</div>}</form></section>}
