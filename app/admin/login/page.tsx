 'use client';
import {useState} from 'react';import {useRouter} from 'next/navigation';
export default function Login(){const [email,setEmail]=useState('');const [password,setPassword]=useState('');const [error,setError]=useState('');const [loading,setLoading]=useState(false);const router=useRouter();async function submit(e:React.FormEvent){e.preventDefault();setLoading(true);setError('');const r=await fetch('/api/admin/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email,password})});const d=await r.json();if(!r.ok)setError(d.error||'Login failed');else router.push('/admin/dashboard');setLoading(false)}return <div className="admin-login"><form className="admin-login-card admin-form" onSubmit={submit}><img src="/logo/virvaani-art-logo.png" width="70" alt="VIRVAANI ART"/><h1>Admin Control Room</h1><p className="admin-muted">Manage VIRVAANI ART content, enquiries and SEO.</p><label>Email<input type="email" value={email} onChange={e=>setEmail(e.target.value)} required/></label><label>Password<input type="password" value={password} onChange={e=>setPassword(e.target.value)} required/></label>{error&&<div className="admin-error">{error}</div>}<button className="admin-btn" disabled={loading}>{loading?'Signing in…':'Sign in'}</button></form></div>}
