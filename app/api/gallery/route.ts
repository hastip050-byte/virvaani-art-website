import {NextResponse} from 'next/server';import {prisma,ensureSeeded} from '@/lib/db';export const dynamic='force-dynamic';export async function GET(){await ensureSeeded();return NextResponse.json(await prisma.galleryItem.findMany({orderBy:{createdAt:'desc'}}))}