import {NextResponse} from 'next/server';import {prisma} from '@/lib/db';import crypto from 'crypto';
export async function POST(req:Request){try{const b=await req.json();if(!b.name||!b.email||!b.message)return NextResponse.json({error:'Name, email and message are required'},{status:400});const row=await prisma.enquiry.create({data:{id:crypto.randomUUID(),name:String(b.name),email:String(b.email),phone:String(b.phone||''),company:String(b.company||''),projectType:String(b.projectType||''),city:String(b.city||''),message:String(b.message),status:'NEW'}});return NextResponse.json({ok:true,id:row.id})}catch(e){console.error(e);return NextResponse.json({error:'Unable to save enquiry'},{status:500})}}
