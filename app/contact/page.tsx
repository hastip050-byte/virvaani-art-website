import Header from '@/components/Header'; import Footer from '@/components/Footer'; import PageHero from '@/components/PageHero'; import ContactForm from '@/components/ContactForm'; import {prisma,ensureSeeded} from '@/lib/db';
export const dynamic='force-dynamic';
export default async function Contact(){await ensureSeeded();const s=await prisma.siteSetting.findUnique({where:{id:'main'}});return <><Header/><PageHero eyebrow="Contact VIRVAANI ART" title="LET'S CREATE SOMETHING PEOPLE REMEMBER." desc="Have a school, public-space or commercial art requirement? Tell us what you want to transform." image="/images/nature-water-cycle.jpg"/><main><section className="section"><div className="container contact-grid"><div className="contact-info"><span className="eyebrow">Contact us here</span><h2>Have a project in mind?</h2><p>Get in touch — we're here to help.</p><div className="contact-block"><b>OUR ADDRESS</b><span>{s?.address}</span></div><div className="contact-block"><b>OUR MAILBOX</b><a href={`mailto:${s?.email}`}>{s?.email}</a></div><div className="contact-block"><b>OUR PHONE</b><a href={`tel:${s?.phone}`}>{s?.phone}</a></div></div><ContactForm/></div></section></main><Footer/></>}
