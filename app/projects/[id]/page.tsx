import Header from '@/components/Header'; import Footer from '@/components/Footer'; import Link from 'next/link'; import {notFound} from 'next/navigation'; import {prisma,ensureSeeded,parseJsonArray} from '@/lib/db';
export const dynamic='force-dynamic';
export default async function ProjectDetail({params}:{params:{id:string}}){await ensureSeeded();const p=await prisma.project.findUnique({where:{id:params.id}});if(!p)return notFound();const scope=parseJsonArray(p.scope);return <><Header/><main><section className="detail-hero"><div className="container detail-grid"><div><span className="eyebrow">Project Detail / {p.year}</span><h1>{p.title}</h1><p>{p.detail}</p><div className="detail-meta"><span><b>Sector</b>{p.sector}</span><span><b>Location</b>{p.location}</span></div><Link href="/contact" className="btn dark">Discuss a Similar Project ↗</Link></div><div className="detail-image"><img src={p.image} alt={p.title}/></div></div></section><section className="section"><div className="container detail-content"><div><span className="eyebrow">Scope of Work</span><h2>FROM IDEA TO FINISHED WALL.</h2><p className="body-copy">Every project is planned around the space, audience, artwork purpose and execution requirements. VIRVAANI ART can take a project from visual direction through on-site artist execution and final quality review.</p></div><div className="scope-list">{scope.map((s,i)=><div key={s}><b>0{i+1}</b><span>{s}</span></div>)}</div></div></section></main><Footer/></>}
