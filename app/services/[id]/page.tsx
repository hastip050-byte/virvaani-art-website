import Header from '@/components/Header'; import Footer from '@/components/Footer'; import Link from 'next/link'; import {notFound} from 'next/navigation'; import {prisma,ensureSeeded,parseJsonArray} from '@/lib/db';
export const dynamic='force-dynamic';
export default async function ServiceDetail({params}:{params:{id:string}}){await ensureSeeded();const s=await prisma.service.findUnique({where:{id:params.id}});if(!s)return notFound();const tags=parseJsonArray(s.tags),deliverables=parseJsonArray(s.deliverables);return <><Header/><main><section className="detail-hero"><div className="container detail-grid"><div><span className="eyebrow">Service / {s.number}</span><h1>{s.title}</h1><p>{s.detail}</p><div className="tags detail-tags">{tags.map(t=><span key={t}>{t}</span>)}</div><Link href="/contact" className="btn dark">Request This Service ↗</Link></div><div className="detail-image"><img src={s.image} alt={s.title}/></div></div></section><section className="section"><div className="container detail-content"><div><span className="eyebrow">What We Deliver</span><h2>BUILT FOR REAL-WORLD EXECUTION.</h2><p className="body-copy">From concept planning to on-site finishing, our team can structure the artwork around your wall size, audience, location and timeline.</p></div><div className="scope-list">{deliverables.map((x,i)=><div key={x}><b>0{i+1}</b><span>{x}</span></div>)}</div></div></section></main><Footer/></>}
