import Link from 'next/link';
import {Service,Project} from '@/lib/data';
export function ServiceCard({s}:{s:Service}){return <article className="card"><Link href={`/services/${s.id}`} className="card-image"><img src={s.image} alt={s.title}/><span className="card-number">{s.number}</span><span className="image-detail">View details ↗</span></Link><div className="card-body"><div className="tags">{s.tags.map(t=><span key={t}>{t}</span>)}</div><h3>{s.title}</h3><p>{s.short}</p><Link href={`/services/${s.id}`}>Explore service <i>↗</i></Link></div></article>}
export function ProjectCard({p}:{p:Project}){return <article className="project-card"><Link href={`/projects/${p.id}`} className="project-image"><img src={p.image} alt={p.title}/><div><span>{p.sector}</span><span>{p.year}</span></div><span className="image-detail">Open project ↗</span></Link><div className="project-body"><small>{p.location}</small><h3>{p.title}</h3><p>{p.summary}</p><Link href={`/projects/${p.id}`}>View full project <i>↗</i></Link></div></article>}
